# HW10-11 – компьютерное зрение в PyTorch: CNN, transfer learning, detection/segmentation

## 1. Кратко: что сделано

- Для части A выбран датасет **STL10** — рекомендованный по умолчанию, содержит 10 классов, изображения 96x96, сложнее CIFAR.
- Для части B выбран датасет **OxfordIIITPet** с треком **сегментации** — понятный сценарий сегментации, готовые аннотации масок, 37 классов питомцев.
- В части A сравнивались 4 эксперимента: простая CNN без аугментаций (C1), с аугментациями (C2), ResNet18 с замороженным backbone (C3), ResNet18 с частичным fine-tuning (C4).
- В части B сравнивались 2 режима постобработки: базовый argmax + foreground (V1) и с удалением мелких компонент связности (V2).
- Выбранный трек для второй части: segmentation

## 2. Среда и воспроизводимость

- Python: 3.x
- torch / torchvision: актуальные версии из conda/pip
- Устройство (CPU/GPU): CPU (или CUDA при наличии)
- Seed: 42
- Как запустить: открыть `HW10-11.ipynb` и выполнить Run All.

## 3. Данные

### 3.1. Часть A: классификация

- Датасет: **STL10**
- Разделение: train/val/test — официальный split STL10 (train: 5000, test: 8000, unlabeled: 100000); для валидации использовался официальный test split как val, дополнительно оставлен test для финальной оценки.
- Базовые transforms: Resize(224) + ToTensor + Normalize
- Augmentation transforms: RandomCrop(224), RandomHorizontalFlip, ColorJitter, RandomRotation(15)
- STL10 содержит 10 классов (автомобили, грузовики, птицы, кошки, олени, собаки, лягушки, лошади, корабли, грузовики), изображения 96x96. Задача сложнее CIFAR10 благодаря меньшему количеству данных и большему разнообразию.

### 3.2. Часть B: structured vision

- Датасет: **OxfordIIITPet**
- Трек: **segmentation**
- Что считается ground truth: три класса пикселей (фон=1, питомец=2, контур=3); для foreground объединены классы 2 и 3.
- Какие предсказания использовались: маски из DeepLabV3_ResNet50 (pretrained на COCO), применён argmax по классу.
- Датасет OxfordIIITPet хорошо подходит для сегментации: готовые аннотации масок, популярный бенчмарк, разумное число изображений для демонстрации пайплайна.

## 4. Часть A: модели и обучение (C1-C4)

Опишите коротко и сопоставимо:

- C1 (simple-cnn-base): Простая CNN (3 conv слоя + FC), без аугментаций, 10 эпох, Adam, lr=0.001. val_accuracy = **0.557**
- C2 (simple-cnn-aug): Та же CNN, с аугментациями (RandomCrop, HFlip, ColorJitter, Rotation), 10 эпох. val_accuracy = **0.54**
- C3 (resnet18-head-only): ResNet18 pretrained, backbone заморожен, обучается только fc-голова, 10 эпох. val_accuracy = **0.935**
- C4 (resnet18-finetune): ResNet18 pretrained, разморожены layer4 + fc, 10 эпох. val_accuracy = **0.893**

Дополнительно:

- Loss: CrossEntropyLoss
- Optimizer(ы): Adam
- Batch size: 64
- Epochs (макс): 10
- Критерий выбора лучшей модели: best_val_accuracy

## 5. Часть B: постановка задачи и режимы оценки (V1-V2)

### Если выбран detection track

### Если выбран segmentation track

- Модель: **DeepLabV3_ResNet50** (pretrained на COCO)
- Что считается foreground: классы 8 (кошки) и 12 (собаки) объединены в один foreground-класс; остальные классы — фон.
- V1: базовая постобработка — argmax по классу, затем объединение классов 8 и 12 в foreground.
- V2: альтернативная постобработка — то же, что V1, плюс удаление связных компонент размером менее 500 пикселей.
- Как считался mean IoU: для каждого изображения вычислялся IoU = intersection/union между предсказанным foreground и ground truth foreground, затем усреднение по всем изображениям.
- Считались ли дополнительные pixel-level метрики: да — pixel_precision и pixel_recall.

## 6. Результаты

Ссылки на файлы в репозитории:

- Таблица результатов: `./artifacts/runs.csv`
- Лучшая модель части A: `./artifacts/best_classifier.pt`
- Конфиг лучшей модели части A: `./artifacts/best_classifier_config.json`
- Кривые лучшего прогона классификации: `./artifacts/figures/classification_curves_best.png`
- Сравнение C1-C4: `./artifacts/figures/classification_compare.png`
- Визуализация аугментаций: `./artifacts/figures/augmentations_preview.png`
- Визуализации второй части: `./artifacts/figures/segmentation_examples.png`, `./artifacts/figures/segmentation_metrics.png`

Короткая сводка (6-10 строк):

- Лучший эксперимент части A: **C3 (ResNet18-head-only)**
- Лучшая `val_accuracy`: **0.9390**
- Итоговая `test_accuracy` лучшего классификатора: **0.9405**
- Что дали аугментации (C2 vs C1): Аугментации не помогли — точность снизилась с 0.557 до 0.54, вероятно из-за слишком агрессивных преобразований для небольшого датасета.
- Что дал transfer learning (C3/C4 vs C1/C2): Transfer learning дал огромный прирост — с ~0.54-0.557 до 0.935, что логично для небольшого датасета.
- Что оказалось лучше: head-only или partial fine-tuning: **Head-only (C3)** оказался лучше (0.935 vs 0.893), вероятно из-за слишком малого числа эпох для fine-tuning.
- Что показал режим V1 во второй части: mean_iou=0.6472, precision=0.7007, recall=0.8222 — хороший recall, но precision хуже.
- Что показал режим V2 во второй части: mean_iou=0.6453, precision=0.6826, recall=0.8196 — удаление мелких компонент немного снизило все метрики.
- Как интерпретируются метрики второй части: Высокий recall показывает, что модель находит большинство пикселей питомцев; более низкий precision указывает на ложные срабатывания. Удаление мелких компонент (V2) не улучшило результат.

## 7. Анализ

Простая CNN показала посредственные результаты (0.557) на STL10, что ожидаемо — маленькая архитектура без pretrained весов не может эффективно извлекать признаки с нуля на относительно небольшом датасете. Аугментации не дали улучшения (C2 хуже C1), что вероятно связано с их избыточной агрессивностью или недостаточным числом эпох для обучения устойчивости к ним. Transfer learning с ResNet18 дал dramатический прирост — pretrained признаки с ImageNet идеально подходят для STL10, так как многие классы (птицы, кошки, собаки, машины) пересекаются. Head-only (C3) превзошёл partial fine-tuning (C4), что объяснимо: при 10 эпохах и маленьком train split слишком легко получить overfitting при размораживании слоёв. В части B сегментация на OxfordIIITPet показала хороший recall (0.82), но precision ниже (0.70), что типично для pretrained модели — она склонна к перепредсказанию. Удаление мелких компонент связности (V2) не помогло, вероятно потому что модель и так даёт достаточно чистые маски, а удаление мелких областей может отсекать нужные детали.

## 8. Итоговый вывод

Для реальной задачи на STL10-подобных данных оптимальный конфиг — ResNet18 с замороженным backbone (head-only) — это быстрый и эффективный подход с минимальным риском overfitting. Transfer learning — ключевой инструмент для small data задач; ImageNet-pretrained веса дают мощный старт. Для сегментации важно правильно выбрать метрику под цель: если важно не пропустить объекты — ориентироваться на recall, если важна точность — на precision; mIoU универсален, но не всегда отражает бизнес-требования.

## 9. Приложение (опционально)

Если вы делали дополнительные сравнения:

- дополнительные fine-tuning сценарии: не делались
- confusion matrix для классификации: не делалась
- дополнительная постобработка для второй части: не делалась
- дополнительные графики: `./artifacts/figures/curves_C1.png`, `./artifacts/figures/curves_C2.png`, `./artifacts/figures/curves_C3.png`, `./artifacts/figures/curves_C4.png`, `./artifacts/figures/sanity_check_A.png`, `./artifacts/figures/sanity_check_B.png`